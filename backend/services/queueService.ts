/* Fonctions à coder : 
- addToQueue(entry) -> retourne position
- removeFromQueue(userId, roomId)
- getQueue(roomId) -> retourne liste des utilisateurs dans la queue
getNextInQueue(roomId) -> retourne le prochain utilisateur dans la queue
*/